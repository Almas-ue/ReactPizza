import {
  BlockInfo,
  HeaderBlockInfo,
  TitleInfo,
} from "@/components/ui/blockInfo";

const Adress = () => {
  return (
    <BlockInfo miniSize={false}>
      <HeaderBlockInfo>
        <TitleInfo>3. Адрес доставки</TitleInfo>
      </HeaderBlockInfo>
    </BlockInfo>
  );
};

export default Adress;
